{
  "name": "dpdp-sentinel-quest",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/compile-ink.cjs && vite",
    "build": "node scripts/compile-ink.cjs && tsc && vite build",
    "preview": "vite preview",
    "ink": "node scripts/compile-ink.cjs"
  },
  "dependencies": {
    "inkjs": "^2.3.0",
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
